# Dynamic Session Demo

Dynamic Sessions are useful for short-lived jobs where explicit rotation is acceptable after timeout, 403, 429, or similar failures.

This example may create a real paid Session.

```bash
pip install requests
export NEXALAYER_API_KEY=agk_your_key
python main.py
```

