# Node Quick Start

Raw API Node example. It creates a dynamic Session, reports telemetry, reads health, and terminates the Session in cleanup.

```bash
cp .env.example .env
export NEXALAYER_API_KEY=agk_your_key
npm start
```

This example may create a real paid Session.

